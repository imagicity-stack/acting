import { ActorLayout } from '@/components/actor/ActorLayout';

export default function ActorRootLayout({ children }: { children: React.ReactNode }) {
  return <ActorLayout>{children}</ActorLayout>;
}
